services:
  papra:
    container_name: papra
    image: ghcr.io/papra-hq/papra:latest
    restart: unless-stopped
    ports:
      - "12210:1221"
    env_file:
      - .env
    volumes:
      - /home/pi/services/papra/app-data:/app/app-data
    user: "${UID}:${GID}"
